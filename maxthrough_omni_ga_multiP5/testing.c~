#include "mex.h"
#include "matrix.h"
void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	int i,j,k,freq;
    double tempsu;
    double* temppu;
    mxArray *rhs;
    rhs = mxDuplicateArray(prhs[0]);
    temppu = mxGetPr(rhs);
     
}